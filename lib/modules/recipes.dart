
class Recipe{

    final String ? _title;
    final String ? _instructions;
    final String ? _category;


    Recipe(this._title, this._instructions,this._category);


    get getTitle => _title;
    get getInstruction => _instructions;
    get getCategory => _category;




    
}